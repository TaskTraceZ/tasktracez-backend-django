version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: tasktracez-backend
    container_name: tasktracez-backend
    command: sh -c "python manage.py migrate && python manage.py runserver 0.0.0.0:8000"
    volumes:
      - tasktracez-data:/tasktracez-backend
    ports:
      - "8000:8000"

volumes:
  tasktracez-data:
